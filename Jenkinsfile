pipeline {
	agent any

	stages {
		stage('Build') {
				steps {
					echo 'Building phase'
					sh 'npm install'
				      }
			}

		stage('Deployment') {
				steps {
					echo 'Deployment phase'
				      }
			}
		}
}
